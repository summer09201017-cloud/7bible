# Roadmap — 多譯本聖經查詢

最後更新:2026-08-10。線上:https://7bible.netlify.app/(push main 自動部署;SW **v25**)

## ✅ 已完成（別重做）

**2026-08-10（DEYI 機；v24→v26，上線收斂驗過）**
- ⬆ **v26 回頂鈕＋經文二次貼邊**（使用者點名兩件）：①回頂鈕與 8bible v79 同款——捲超過 1.5 個
  螢幕才出現、rAF 節流、48px 圓鈕右下角固定（詩119 十二萬 px 一鍵回頂實測過）;
  ②手機經文再貼邊：卡內距 6→2、經文框內距 8/10→4/5，行寬 352→376px＝每列約 23→25 個中文字
  （v24 起累計 19→25，+31%）。sw v26。
- 📖 **v25 站內大字註釋面板**（使用者點名「像 8bible 手機版的大字註釋」）：「註釋」鈕從外跳信望愛
  改成行內面板（互動同串珠/原文）——sc.php JSON 原生排版（⚠ sc.php 書卷參數**只能用 engs=**，
  chineses=約 會回羅馬書，與 qb.php 正好相反）、reflowComText 硬換行重排成自然段（8bible v84 同款）、
  `#參照|` 轉站內查詢、**SNG/SNH 原文編號點了就地展開中文字義**（吃 v23 同一個字典快取）、
  上一段/下一段導航、字級跟隨字級滑桿（大字）、CBOL 版權標示＋「在信望愛開啟」逃生口。
  共用 core 升級：`src/lib/commentary-core.js`＋`strongs.js` 改從 **skill `fhl-bible-api` 正本**同步
  （configureStrongs 沿用既有快取鍵）；簡易模式自動隱藏；章節/關鍵字兩檢視都有；sw v25。
- 📱 **v24 手機結果卡滿版**（使用者點名「卡片邊界向兩邊擴展到螢幕邊」）：≤768px 時結果卡
  `.result-bleed` 負邊距 -16px 抵銷容器留白貼齊螢幕邊，卡內距 16→6、經文框內距 12/14→8/10；
  經文行寬 298→352px＝每列約 19→23 個中文字（+18%）。桌機不受影響（media query 內）；
  Playwright 驗過章節/關鍵字兩檢視無橫向捲動；sw v24。

**2026-08-09 晚（DEYI 機；v23，上線收斂驗過）**
- 🔤 **v23 Strong's 原文編號**（大表「讀6」完工＝閱讀站近期佇列清空，剩排後的讀7 聽經播放器/讀8 語音輸入）：逐節「原文」鈕（章節/關鍵字兩檢視都有，
  簡易模式 adv-tool 自動隱藏）→ 行內面板顯示帶編號的和合本，點詞看中文字義（希臘文/希伯來文/文法碼
  全可查）；字典內 `#出 34:10|` 式引用可點直跳站內查詢；◦＝原文有、中文未譯出的字。
  **資料源拍板＝Plan A「FHL 線上」**（qb.php `version=unv&strong=1` ＋ sd.php `N=0新約/1舊約`；
  實測 CORS 全通——sd.php 回顯 Origin、sc.php 給 `*`，「CORS 風險」不成立；整章 0.1s/15KB；
  KJV+Strong's 離線包否決＝釋義全英文＋標籤掛在 KJV 英文字上，做不出「點和合本中文詞」）。
  共用 core 檔 `src/lib/strongs.js`（零 React、可垂直搬運到 8bible；勿就地分岔）；字典 localStorage
  LRU 200 條（查過的字離線可重看；是快取不進備份鏈）；FHL 請求走 SW 既有直通分支不快取；sw v23。
  ⚠ 地雷：FHL `engs=` 參數實測回錯書卷（engs=John 回羅馬書），一律用 `chineses=` 中文縮寫。

**2026-08-09 下午第二批（v18→v22，全上線收斂驗過）**
- 🔊 **v18 破音字字典**（共用 core 第一步）：`src/lib/tts-fix.js`＝web-speech-scripture 正本原封搬運
  （標頭註明勿就地改），即時朗讀＋存語音兩路 zh 先過 toSpeakable。
- 💾 **v19 備份提醒**（拍板走輕量）：有像樣資料（足跡≥7天或歷史≥10筆）且 30 天沒匯出 → 查詢足跡卡
  頂提醒條（立即匯出／7 天後再提醒）；匯出記時間戳；裝置層鍵不進備份。
- 🧓 **v20 簡易模式**＋體檢黃燈二修：搜尋列一鍵「簡易/回完整」收起查經工具（進階搜尋/差異高亮/
  串珠/註釋/複製格式/排序箭頭）；viewport 拿掉 user-scalable=no（可捏合放大）；UI 小字 12/13→14/15。
- 📅 **v21 讀經計畫 1/2/3 年＋自訂年數**：雙制並存——沒設定＝原年曆制原樣（零遷移）；自訂計畫
  打卡鍵 `plan-{start}-` 前綴隔離；⚙ 換計畫選單；planConfig 接匯出/匯入。
- 🔍 **v22 關鍵字結果上下文預覽**：「⊕ 前後文」原地展開前後各 2 節（與 8bible v87 同一互動模式）；
  api.js 補 export fetchLocalVersion。

**2026-08-09 上午（agape250 機；sw v17，上線收斂驗過）——0809 雙站體檢修復包**
- 🔖 **譯本偏好防蓋三入口**（使用者原話「每次都要手動勾選」；本站勾選本來就會存，但有三個入口會蓋掉）：
  ①歷史重跑改「用我現在的譯本組合再查」（不再 setVersions 回舊組合）②網址列 replaceState 不再寫 `v=`
  （舊書籤/分享連結不再夾帶譯本組合）③冷啟動 `?v=` 深連結只作用當次查詢；譯本區加提示「勾了會自動記住」。
- ⚠ **清空查詢歷史加 confirm**（審查 A2：原本一鍵紅按鈕直接全清）。
- 🔊 **朗讀速度/朗讀譯本兩鍵接進備份鏈**（審查 A3：`sevenbible-speak-rate`/`-speak-version` 併入
  exportData/importData 的 settings；匯入 raw 寫回、元件下次讀取生效）。
- 💣 **本機 dist/ 不可信**入 HANDOFF 地雷區（審查 A1.5：dist 曾停在 v12 而線上 v16，
  手滑拖拉部署=整站倒退；已刪殘留，dist 本來就在 .gitignore）。
- 📋 0809 立「📖 閱讀站佇列」於 skills repo 大表＝跨站待辦單一真相；本表下面只列本站的。

**核心**
- 8 譯本並排對照（和合本 / NIV / ESV / 新譯本 / 呂振中 / ASV / KJV；WEB 出貨但 UI 隱藏）
- 章節查詢（約 3:16 / 創 1 / John 3:16-18）與關鍵字查詢（AND/OR、精確片語、排除詞、舊約/新約/自訂範圍）
- 關鍵字結果二次篩選（範圍 / 書卷 / 譯本）、分頁「載入更多」
- 差異高亮（同語系才比對，可指定比較基準）
- 譯本可開關、可用 ◀▶ 排序（順序＝顯示順序）

**個人化 / 工具**
- 逐節複製 / 全選複製（四種格式：純文字 / 單行 / Markdown / HTML）
- 朗讀（Web Speech）、匯出經文卡 PNG、分享到 Line / Email
- 續讀書籤、查詢歷史、365 天每日讀經進度打卡
- 主題：淺色 / 護眼 / 夜讀 / 跟隨系統
- 匯出 / 匯入 JSON（含歷史、足跡、進度、全部設定）

**Wave 3（2026-07-19）**
- 分頁化：查詢頁 / 讀經進度與足跡頁（搜尋自動切回查詢頁）
- **讀經足跡**（reading-footprint 範式）：整章停留 15 秒才記、啟動還原/單節/搜尋不計、同章 30 分防重、月分桶、統計卡 + 範圍 chips + 熱區前 10 + 全卷熱圖（點格子跳章）
- **串珠交叉引用**：openbible.info（CC-BY），29,318 節、每節最多 10 條、分 66 卷懶載入
- IME 組字防誤觸搜尋、關鍵字命中數修正（非搜尋譯本顯示「對照」）
- `structure.json` 輕量啟動（5 KB 取代 4 MB 和合本全文）
- toast 取代 window.alert
- 移除：每日金句、背經模式、個人筆記/收藏/螢光筆（使用者要求）

## 🔜 待做（0809 晚收工版；跨站順序看大表「📖 閱讀站佇列」）

1. **搜尋移到 Web Worker + 簡單索引** — ★★★ ⏱ 1–2 天。根治多譯本全書掃描卡頓；NIV 各卷改平行下載。
2. **語音輸入搜尋**（Web Speech Recognition）— ★★ ⏱ 半天（大表「讀8」）。
3. **關鍵字分佈圖** — ★★ ⏱ 半天。
4. **一鍵預快取全部譯本供離線** — ★★ ⏱ 半天。

> 0809 完工移出：Strong's 原文編號（v23，讀6 完工）、上下文預覽（v22）、破音字字典（v18）、
> 備份提醒（v19）、簡易模式（v20）、讀經計畫多年版（v21）——見上方已完成段。

## 🚫 足跡清除鈕：0809 使用者終局拍板＝**不補**（案結）

- 理由（使用者原話大意）：足跡珍貴，都不需要清除，反而需要刻意的**備份與移交給另一台 PC**。
- 同日 8bible 連既有的清除鈕＋函式都拿掉了（sw v83）；本站維持「無停用、無清除」現狀。
- Opus 5 二審「補清除」主張經使用者否決。體檢報告那條紅燈以本拍板為準（刻意設計，已白紙黑字）。
- 衍生方向＝「備份與移交」：見待做 4 備份提醒；未來可加「一鍵匯出→新機匯入」的搬家導引。

## 🚫 刻意不做（除非使用者明確要求）

- **重新加回**每日金句 / 背經模式 / 個人筆記（Wave 3 已依使用者要求移除）
- 拆分 App.jsx 成多檔、遷移到 Tailwind、加自動化測試
- AI 釋義（Claude API）— 需 proxy + 消費上限，與離線原則衝突，成本考量下最後再議
- 靈修日記、統計圖表（優先度低）
